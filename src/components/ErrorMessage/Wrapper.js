import styled from 'styled-components';

const Wrapper = styled.div`
  padding-top: 11px;
  color: #ff203c;
  font-size: 13px;
  font-family: 'Lato';
  > span {
    display: inline-box;
    margin-bottom: 14px;
  }
`;

export default Wrapper;
